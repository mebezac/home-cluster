apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: umami
  namespace: argo-system
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: kubernetes
  sources:
    - repoURL: https://github.com/mebezac/home-cluster.git
      path: kubernetes/apps/umami/umami
      targetRevision: main
      ref: umami-repo
    - repoURL: ghcr.io/bjw-s-labs/helm
      chart: app-template
      targetRevision: 4.3.0
      helm:
        releaseName: umami
        valueFiles:
          - $umami-repo/kubernetes/apps/umami/umami/values.yaml
  destination:
    name: in-cluster
    namespace: umami
  syncPolicy:
    automated:
      allowEmpty: true
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
