apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bothy
  namespace: argo-system
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: kubernetes
  sources:
    - repoURL: https://github.com/mebezac/home-cluster.git
      path: kubernetes/apps/bothy/bothy
      targetRevision: main
      ref: bothy-repo
    - repoURL: ghcr.io/bjw-s-labs/helm
      chart: app-template
      targetRevision: 5.0.1
      helm:
        releaseName: bothy
        valueFiles:
          - $bothy-repo/kubernetes/apps/bothy/bothy/values.yaml
  destination:
    name: in-cluster
    namespace: bothy
  syncPolicy:
    automated:
      allowEmpty: true
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
