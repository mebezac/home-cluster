controllers:
  calibre-web-automated:
    containers:
      app:
        image:
          repository: crocodilestick/calibre-web-automated
          tag: V3.1.1@sha256:b84377e5c8e559ab80c803dd38d96087cf92b48206cefcc749d521d141e0b8e7
        env:
          CACHE_DIR: /cache
          PUID: 1000
          PGID: 1000
          TZ: America/New_York
        probes:
          liveness: &probes
            enabled: true
            custom: true
            spec:
              httpGet:
                path: /login
                port: &port 8083
              initialDelaySeconds: 0
              periodSeconds: 10
              timeoutSeconds: 1
              failureThreshold: 3
          readiness: *probes
        securityContext:
          allowPrivilegeEscalation: false
        resources:
          requests:
            cpu: 5m
            memory: 128Mi
          limits:
            memory: 512Mi

service:
  app:
    ports:
      http:
        port: 8083

ingress:
  app:
    enabled: true
    className: internal
    hosts:
      - host: calibre.laboratory.casa
        paths:
          - path: /
            service:
              identifier: app
              port: http

persistence:
  data:
    enabled: true
    type: persistentVolumeClaim
    storageClass: longhorn
    accessMode: ReadWriteOnce
    suffix: data
    size: 128Mi
    globalMounts:
      - path: /config
  cache:
    enabled: true
    type: persistentVolumeClaim
    storageClass: longhorn
    accessMode: ReadWriteOnce
    suffix: cache
    size: 512Mi
    globalMounts:
      - path: /cache
  media:
    enabled: true
    type: persistentVolumeClaim
    storageClass: freenas-api-nfs
    accessMode: ReadWriteMany
    size: 2Gi
    globalMounts:
      - path: /calibre-library
        subPath: library
      - path: /cwa-book-ingest
        subPath: ingest
