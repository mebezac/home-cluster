apiVersion: v1
kind: Secret
metadata:
  name: podcast-adblock-secret
  namespace: podcasts
type: Opaque
stringData:
  # Add your secrets here, then encrypt with sops
  # Example: API_KEY: your-api-key
