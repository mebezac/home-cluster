serviceMonitor:
  enabled: true
  labels:
    release: kube-prometheus-stack

grafanaDashboard:
  enabled: true

manager:
  resources:
    limits:
      cpu: 500m
      memory: 128Mi
    requests:
      cpu: 5m
      memory: 64Mi
