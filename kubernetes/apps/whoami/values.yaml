ingress:
  annotations:
    external-dns.alpha.kubernetes.io/target: external.laboratory.casa
  enabled: true
  ingressClassName: "external-zac-pizza"
  hosts:
    - host: "whot.zac.pizza"
      paths:
        - "/"
