ingress:
  # annotations:
  #
  #   external-dns.alpha.kubernetes.io/target: external.zac.pizza
  enabled: true
  ingressClassName: "internal"
  hosts:
    - host: "whot.zac.pizza"
      paths:
        - "/"
